<!DOCTYPE html>
<html>
  <head>
    <title>Suv</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', 'https://fonts.googleapis.com/css2?family=Zen+Dots&display=swap'%>
  </head>

  <body>

    <header>
    
      <div id="logo">
        <div><%= link_to "Best SUV", root_path, id: "logo1" %></div>
        <div><%= link_to "2021", root_path, id: "logo2" %></div>
        
      </div>

      <div id="loginout"> 
        <% if current_user %>
          <div id="login">Logged in as <%= current_user.email %></div>
          <div><%= link_to "Logout", logout_path, id: "logout" %></div>
        <% else %>
          <div id="register"><%= button_to "Login or SignUp", '/login', class: "myButton" %></div>
        <% end %>

      </div>


    </header>

   

    <div id="container">
    
    <%= yield %>
    </div>
  </body>
</html>
